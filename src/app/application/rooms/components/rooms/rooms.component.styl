:host
  display flex
  height 100%
  flex-direction column
  padding 2rem
  width 100%

.button-container
  display flex
  justify-content space-between
  min-height 55px
  margin-bottom 2rem
  width 100%