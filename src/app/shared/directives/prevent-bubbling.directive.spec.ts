import { PreventBubblingDirective } from './prevent-bubbling.directive';

describe('PreventBubblingDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventBubblingDirective();
    expect(directive).toBeTruthy();
  });
});
